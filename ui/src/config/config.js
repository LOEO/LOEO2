import Env from './env';

let config = {
    name: 'LOEO',
    env: Env,
    baseUrl: 'http://localhost:8080',
    serviceUrl: 'http://localhost:8888/api',
    ajaxTimeout: 60000,
};
export default config;